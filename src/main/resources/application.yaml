spring:
  application:
    name: controle-medicamento
  security:
    user:
      name: admin
      password: adminpass
      roles: ADMIN
  datasource:
    url: ${DATABASE_URL}
    user: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
server:
  port: 8081